import { decodeToken, colorFormat } from "./varFormat";
import {
  convertPhoneNumberGlobal,
  convertPhoneNumberNation,
  hidePhoneNumber,
} from "./convertPhoneNumber";
import { convertCurrency, convertDiscount } from "./convertCurrency";
export {
  decodeToken,
  colorFormat,
  convertPhoneNumberGlobal,
  convertPhoneNumberNation,
  hidePhoneNumber,
  convertCurrency,
  convertDiscount,
};
